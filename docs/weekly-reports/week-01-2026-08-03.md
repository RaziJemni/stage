# Week 01 Report

**Period:** 2026-07-30 to 2026-08-03
**Prepared by:** Calendar implementation stream
**Iteration goal:** Deliver the first persistent iCalendar import path for Issue #3.

## Completed and Merged

| Issue / PR | Requirement IDs | Result | Verification |
|---|---|---|---|
| #3 / PR #47 | FR-CAL-01, FR-CAL-02, NFR-REL-01, NFR-PERF-03 | iCalendar feed configuration, asynchronous import, sync history, and safety controls merged into `testing`. | Backend tests and a public-feed manual import passed before merge. |

## In Progress

| Issue | Owner | Current state | Next step | Blocker |
|---|---|---|---|---|
| #12 | Messaging completion stream | Securing message writes, connecting persistent inbox/thread UI, and adding focused coverage. | Open PR into `testing`. | WhatsApp adapter and chatbot replies remain separate work. |
| #10 | Chatbot safety stream | Added the deterministic escalation policy, versioned multilingual evaluation dataset, and manual-handover persistence. | Open PR into `testing`. | Production chatbot provider remains undecided. |

## Decisions Accepted Elsewhere

- [Decision 0004](../decisions/0004-icalendar-feed-import.md) - iCalendar parsing, asynchronous import, scheduling, idempotency, and URL-safety baseline.
- [Decision 0005](../decisions/0005-chatbot-escalation-policy.md) - controlled chatbot escalation and confirmation baseline.

## Validation Performed

- `docker compose --profile test run --rm backend_test pytest -q tests/test_calendar.py` - 5 passed.
- `docker compose --profile test run --rm backend_test pytest -q` - 29 passed.
- `docker compose up -d --build backend worker beat` - backend health returned `ok`; worker registered both calendar tasks; Beat started.
- Manual local API test imported 27 events from a public iCalendar feed and recorded a succeeded sync run.
- `docker compose --profile test run --rm backend_test pytest -q tests/test_messaging.py` - 3 passed for CSRF, tenant isolation, ordered history, manual takeover, and duplicate inbound messages.
- `docker compose exec -T frontend npm run lint`, `npm test -- --run`, and `npm run build` - lint passed, 18 tests passed, and the production build passed.

## Risks and Blockers

| Risk/blocker | Impact | Owner | Mitigation/next action |
|---|---|---|---|
| Recurring iCalendar events are not expanded in the MVP importer. | Recurring source events are not yet a supported availability input. | Calendar stream | Define recurrence behavior before adding it to supported scope. |
| Feed configuration has no frontend screen yet. | Managers currently configure feeds through the API only. | Calendar/frontend stream | Connect manager configuration and portfolio Calendar UI under Issue #6 or a scoped follow-up. |

## Documentation Updated

- `README.md` - reflects the implemented API-based iCalendar import and Beat service.
- `docs/decisions/0004-icalendar-feed-import.md` - records the accepted technical baseline.
- `.env.example` - exposes calendar interval and feed-size settings.
- Messaging API, inbox/thread UI, and focused tests - complete the persistent manual-takeover workflow while keeping WhatsApp/chatbot delivery truthfully unconfigured.
- Chatbot escalation policy and multilingual evaluation fixture - records mandatory handovers without selecting a chatbot provider or enabling automated replies.

## Planned Next Week

- Complete the secure persistent conversation/thread workflow for Issue #12.
- Start Issue #4 for cancellation, missing-event policy, and synchronization-health behavior after the calendar and booking streams are reconciled.

## Noticed but Out of Scope

- The portfolio Calendar frontend remains mock-data based until Issue #6.
