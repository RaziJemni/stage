# CLAUDE.md

@AGENTS.md

Claude Code does not read AGENTS.md automatically -- this file exists purely
so Claude Code loads the same instructions every other agent in this repo
follows, via the import above. Also read `docs/architecture.md` and
`docs/design-system.md` before starting any task, as AGENTS.md instructs.
